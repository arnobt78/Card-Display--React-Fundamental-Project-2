import React from 'react';

/** Loading – Simple spinner UI. Styled in index.css via .loading (border + animation). */
const Loading = () => {
  return <div className='loading'></div>;
};

export default Loading;
